# ProgramKit.Forms.Management.Mcp.AspNetCore

An independently selected `IShellFeature` that contributes the explicit form-management tool set
to `ProgramKit.Mcp.AspNetCore`. It shares the claims-derived form actor provider with the optional
operational form contributor and maps no endpoint or middleware itself.
